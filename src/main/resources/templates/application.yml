server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/User
    username: postgres
    password: 11111111
    driver-class-name: org.postgresql.Driver

    jpa:
      hibernate:
        ddl-auto: update
        show-sql: true